# V16 Signal Architecture - Candidate Receipt

**Prepared:** 2026-08-05 (Pacific/Honolulu)  
**Canonical repository:** `GlacierEQ/job-application`  
**Release branch:** `release/v16-signal-architecture`  
**Production target:** `https://casey-barton-glaciereq.vercel.app/`  
**State:** `SOURCE_COMPLETE_EXACT_HEAD_GATES_REQUIRED`

## Design objective

Advance the verified V15 hiring system into a cutting-edge, recruiter-grade visual experience without changing factual identity, evidence state, test counts, artifact IDs, limits, blockers, authority boundaries, or non-affiliation language.

## Visual system delivered

- cinematic, outcome-first recruiter hero;
- CSS-only evidence cockpit and rotating radar field;
- proof strip with exact evidence totals;
- bento-grid 45-second recruiter read;
- before/after architecture-repair case study;
- deterministic Observe -> Bound -> Build -> Verify -> Persist flow;
- ten-system flagship hierarchy with Microcode still execution-blocked;
- refined resume with evidence rail, proof cards, and experience timeline;
- terminal-style technical and machine surfaces;
- typed company and evidence mesh;
- responsive, reduced-motion, keyboard-focus, resume-print, and CSP contracts;
- zero client JavaScript and zero tracking.

## PSYSOC-X calibration

The recruiter profile is more direct, human, and outcome-led. Master, machine, and mesh remain progressively denser. All four preserve the same factual graph and explicit truth boundaries.

## Source normalization

Commit `a95a63ffba1108c903b62dff062d7958d3a0fd6b` completed the one-shot source normalization pass:

- repaired mixed UTF-8, CP-1252, and Latin-1 source text;
- emitted deterministic ASCII HTML with numeric entities for display characters;
- corrected the malformed external-actions label;
- verified both shared stylesheet links on every route;
- added white-background technical print overrides;
- removed the temporary normalization workflow from the branch.

This normalization commit was produced by `GITHUB_TOKEN`, so a separate connector-authored receipt commit is used to trigger the exact-head validation suites.

## Deterministic validation contract

The exact-head validator must confirm:

- five canonical routes plus the 404 surface;
- four PSYSOC-X profiles and their exact routes;
- exact proof totals: 69, 166, 19, and zero external actions;
- Receipt Router first and Microcode explicitly `REVIEWED_EXECUTION_BLOCKED`;
- ten unique flagship IDs, exact ranks, and evidence/limit/source fields;
- 27 company families, 200 repositories, 203 memberships, 78 unprocessed items, and 34 reference/upstream items;
- one H1 per page;
- no scripts and no inline styles;
- locked CSP;
- cockpit, radar, bento, terminal, CSS motion, reduced-motion, and resume-print contracts;
- exact resume PDF SHA-256;
- ATS resume identity and minimum content size.

The release workflow derives its receipt fields from the validator output and writes a companion SHA-256 digest. GitHub Actions artifacts are time-limited workflow records; they are not described as cryptographically signed or permanently immutable.

## Promotion boundary

This receipt proves that the source candidate exists and defines its required gates. It does not claim that a future head passed CI, that a Vercel preview matches the source, that production was promoted, or that accessibility certification, performance scores, hiring outcomes, customer adoption, company affiliation, proprietary access, or physical-infrastructure operation were established.
