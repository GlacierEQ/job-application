# `site-v15` is a path name, not a product version

This directory is the **Vercel `outputDirectory`** for GlacierEQ/job-application.

Product version lives in:

- Response headers (`X-Psysocx-Release`, title release, helix/source commits)
- `data/current-proof.json` (e.g. V21 First Star)
- `data/helix-root.json` + `job-app-helix` manifests
- `data/portfolio.json` → `release` (Unified Helix-Bound Hire Surface)

Do not reintroduce “V15 Final Hiring Release” branding.  
`site-v13` / `site-v14` have been removed; this is the sole static output tree.
