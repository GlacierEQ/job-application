name: Finalize V17 Resume Release

on:
  push:
    branches:
      - release/v16-signal-architecture
    paths:
      - '.github/workflows/v17-finalize-release.yml'
  workflow_dispatch: {}

permissions:
  contents: write

concurrency:
  group: v17-resume-native-finalization
  cancel-in-progress: false

jobs:
  finalize:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - name: Check out exact release branch
        uses: actions/checkout@v4
        with:
          ref: release/v16-signal-architecture
          fetch-depth: 0

      - name: Recover digest-locked V17 source bundle
        run: |
          python site-v15/scripts/v17-bundle-recovery.py
          tar -xzf /tmp/v17-text-bundle.tar.gz -C .
          git restore .github/workflows/v17-resume-intelligence.yml

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install deterministic document generators
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends libreoffice-writer fonts-liberation
          python -m pip install --disable-pip-version-check reportlab==4.4.3 python-docx==1.2.0 pypdf==5.9.0

      - name: Generate exact V17 artifacts natively
        run: python site-v15/scripts/build-resume-v17-deterministic.py

      - name: Validate V16 factual invariance
        run: node site-v15/scripts/validate.mjs

      - name: Validate V17 resume intelligence
        run: node site-v15/scripts/validate-resume-v17.mjs > /tmp/v17-validation.json

      - name: Verify validation receipt
        run: |
          node - <<'NODE'
          const fs = require('node:fs');
          const receipt = JSON.parse(fs.readFileSync('/tmp/v17-validation.json', 'utf8'));
          if (receipt.status !== 'PASS' || receipt.facts_invariant !== true) {
            throw new Error('V17 validation receipt contract failed');
          }
          console.log(JSON.stringify({
            status: receipt.status,
            schema: receipt.schema,
            facts_invariant: receipt.facts_invariant,
            files: receipt.files
          }, null, 2));
          NODE

      - name: Persist finalized V17 release
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add \
            site-v15/scripts/generate-resume-v17.py \
            site-v15/scripts/build-resume-v17-deterministic.py \
            site-v15/resume/ats.txt \
            site-v15/data/resume.json \
            site-v15/data/resume-artifacts.json \
            site-v15/downloads/Casey_Barton_Resume.pdf \
            site-v15/downloads/Casey_Barton_Resume.docx \
            site-v15/machine/index.html \
            RESUME_ATS.md \
            deployment-receipts/V17_RESUME_INTELLIGENCE_FINAL_2026-08-05.md
          if git diff --cached --quiet; then
            echo "Exact V17 release already committed."
          else
            git commit -m "Finalize elite V17 resume intelligence"
            git push origin HEAD:release/v16-signal-architecture
          fi
